#include <stdio.h>

int main()
{
    int n,m,i,j,s,a[2010],b[2010];

    while(scanf("%d %d",&n,&m)==2)
    {
        if(n==0 && m==0){
            break;
        }
        else if(n>m){
            printf("Loowater is doomed!\n");
        }
        else{
            sort(a,a+n);
            sort(b,b+m);

            for()
        }
    }
}
