#include <stdio.h>
#include <math.h>

int main()
{
    float num,ans;

    printf("Enter the number:");
    scanf("%f",&num);

    ans = sqrt(num);

    printf("The square root of that number is %f\n",ans);

    return 0;
}

