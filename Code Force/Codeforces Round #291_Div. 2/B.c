#include <stdio.h>

int main()
{
    long int n,x,y,a[100000],b[100000];

    scanf("%ld",&n);

    while(n--)
    {
        scanf("%ld %ld",&x,&y);

        for(i=0;i<n;i++)
        {
            scanf("%ld %ld",&a[i],&b[i]);
        }
    }

}
